<Page
    x:Class="uwpharjoitukset3.Views.harjoitus3_1a"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:uwpharjoitukset3.Views"
   
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=local:harjoitus3_1a}"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="30">
        <RelativePanel>
            <TextBlock Name="PassWordTextBlock" FontSize="18" Margin="20" Text="{x:Bind PassWord ,Mode=OneWay}" ></TextBlock>
            <Button Name="PassWordButton" RelativePanel.Below="PassWordTextBlock" Margin="20" Click="Button_Click" >TUOTA UUSI SALASANA</Button>
            <Button Name="UselessFactButton" RelativePanel.Below="PassWordButton" Margin="20" Click="UselessFactButton_Click" >CHUCK NORRIS-BUTTON</Button>
            <TextBlock Name="UselessFactTextBlock" RelativePanel.Below="UselessFactButton" FontSize="18" Margin="20" Text="{x:Bind UselessFact ,Mode=OneWay}" TextWrapping="Wrap" ></TextBlock>

        </RelativePanel>
    </Grid>
</Page>
